provider:
  name: openfaas
  gateway: https://faas.srv.disarm.io
functions:
  fn-prevalence-predictor-mgcv:
    lang: r-geospatial
    handler: ./function
    image: disarm/fn-prevalence-predictor-mgcv:0.0.7
    environment:
      write_timeout: 300
      read_timeout: 300
      exec_timeout: 300
      combine_output: false
    labels:
      com.openfaas.scale.zero: true
